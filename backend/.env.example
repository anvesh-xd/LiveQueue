# Server
PORT=3001

# Auth (REQUIRED - generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=

# Admin (REQUIRED - generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ADMIN_SECRET=

# Database
DATABASE_URL="postgresql://user:password@localhost:5432/livequeue?schema=public"
